{
  "name": "dev-workbench-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "index": "python3 ../scripts/build-index.py ..",
    "predev": "npm run index",
    "dev": "astro dev",
    "prebuild": "npm run index",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "npm run index && node tests/search.test.mjs"
  },
  "dependencies": {
    "@astrojs/preact": "^4.1.0",
    "astro": "^5.14.1",
    "marked": "^16.4.0",
    "minisearch": "^7.2.0",
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.2"
  }
}
