node_modules/
.DS_Store
.idea/

# Astro build output and its type cache.
site/dist/
site/.astro/

# Written by scripts/build-index.py on every build. Committing it would let the
# catalogue drift from plugins/.
site/src/data/
site/public/data/

# Working notes. docs/tmp/README.md says the directory is meant to be deleted before it
# matters — until then it stays out of the repository, because two of its files name a
# former employer, two private repositories and a local absolute path.
docs/tmp/
