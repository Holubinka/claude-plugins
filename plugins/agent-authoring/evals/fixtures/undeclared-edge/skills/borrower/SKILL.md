---
name: borrower
description: "Use when a task needs the review pass that lives in another plugin."
---

# borrower

For the review itself, invoke `drifted-set:review-everything` and act on what it returns.
