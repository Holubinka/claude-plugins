# drifted-set

A deliberately broken set, one file per failure the audit exists to catch. Every problem here has
happened in a real set; none of them makes anything error at runtime, which is why a static check is
the only thing that finds them.
