The description is a numbered procedure. That is the failure the `authoring` skill exists to name.

The response passes only if **all** of the following hold.

1. **It identifies that the description summarises the workflow** rather than stating when the skill
   applies.
2. **It names the consequence specifically**: a description that reads like a procedure gets followed
   *instead of* the body — the model acts on the summarised steps and never loads the file. Saying
   only "it is too long" or "it is unclear" is a **fail**; the length is not the problem.
3. **It offers a replacement written as a trigger**, in the vocabulary someone would actually type —
   preparing a release, a version bump, "is this branch ready to ship" — rather than in the author's.
4. **The replacement contains no step list, and no `--push`-style mechanism.**

Also passing: noting that the steps are not lost, they belong in the body. Not required.
