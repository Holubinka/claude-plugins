Use the `research-tools:investigator` agent to find out why this repository tags releases as
`{plugin}--v{version}` rather than plain semver tags, when that decision was made, and whether the
approach is common practice in other plugin marketplaces.
