I am adding a payments module to this Node service. The existing tree is `src/controllers/`,
`src/services/`, `src/repositories/` — every controller together, every service together. Where do
the payment files go?
