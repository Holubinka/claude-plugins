Run the bundled `ts_diagnostic.py` from `engineering-paved-path`'s `typescript-expert` skill
against `evals/fixtures/no-src-repo`, and tell me what it found. That fixture has no top-level
`src/` directory.
