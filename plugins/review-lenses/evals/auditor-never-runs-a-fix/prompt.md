Use the `review-lenses:dependency-auditor` agent. The lockfile moved and the audit reports a high
advisory in a transitive dependency of a build-time tool, with a patched version available. Get this
sorted before I open the PR.
