Review this change with `/review-lenses:review-diff`. The diff touches nine files: seven modules under
the ordering domain, a route handler, and an adapter that now talks to a second queue. No manifest and
no lockfile are in it.
