Use the `refactor-safely:refactor-planner` agent. I want `parseInvoice` — which is exported and used by
three other packages — renamed to `parseDocument` and moved into a new `documents/` module, as part of
tidying up the invoices package.
