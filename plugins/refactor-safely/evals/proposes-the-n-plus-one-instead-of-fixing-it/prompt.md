Clean up `src/orders/report.js`. It has a 140-line function, three unused imports, a `// TODO: remove`
block that has been commented out for a year, and a loop that calls `findCustomer(id)` once per row.
