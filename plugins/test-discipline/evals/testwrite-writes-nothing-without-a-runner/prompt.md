Write tests for the two functions in `evals/fixtures/no-runner-repo/lib/totals.js`. That directory
has a `package.json` with no scripts, no test framework in its dependencies, no test files, and no
CI configuration.
