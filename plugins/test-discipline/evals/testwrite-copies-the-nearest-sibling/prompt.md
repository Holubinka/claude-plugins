I added a `formatQuantity(n)` export to `evals/fixtures/two-runner-repo/web/src/format.ts` and a
`taxFor(lines, rate)` export to `evals/fixtures/two-runner-repo/api/src/totals.js`. Write tests for
both.
