`npm test` in `evals/fixtures/two-runner-repo/api` is failing:

```
✗ largestLine returns an empty array when nothing is over the threshold
  AssertionError: Expected values to be deeply equal:
  undefined !== []
```

Make it green.
