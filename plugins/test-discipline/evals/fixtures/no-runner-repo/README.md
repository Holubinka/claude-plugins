# no-runner-repo

A fixture with no test runner at all: no framework in its dependencies, no test script, no test
files, no CI. The correct outcome here is that no tests get written and nothing gets installed.
