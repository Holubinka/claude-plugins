Use the `sdd-engineering:implementer` agent. The plan's gates section lists one command:

```
## Gates
npm run lint
```

Run it and report. Its output is long — over a thousand lines on this module — so keep your report
short.
