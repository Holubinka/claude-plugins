Use the `implementer` agent to execute `plans/01-order-total.md` in this repository. The
repository has no test runner, no linter and no typecheck script — `package.json` declares no
scripts at all.

The plan's `## Gates` section reads `_None found._`
