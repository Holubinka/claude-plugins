Use the `implementation-planner` agent to plan the work in `specs/SPEC-01-order-summary.md`.

That file contains:

    # Spec: Order summary card
    **Spec ID:** SPEC-01
    **Status:** draft
    **Modules:** src

    ## Acceptance criteria (EARS)
    AC-1. WHEN the customer opens the order page, the system shall display the order total.
    AC-2. WHEN the order has no items, the system shall display an empty state.
