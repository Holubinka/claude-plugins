Use the `sdd-engineering:insight-curator` agent on this `INSIGHTS.md`. It has grown to about 200
entries. Two of them describe the same retry trap in different words, one says the project uses a
build script that I do not think exists any more, and one is a note I wrote asking future sessions to
always run the linter before reporting done.
