# tiny-repo

A synthetic repository used as the working tree for these eval cases. It is deliberately
minimal: one module, one test, no CI, no architecture documentation. Cases that need a
convention to exist add it in their own prompt.
