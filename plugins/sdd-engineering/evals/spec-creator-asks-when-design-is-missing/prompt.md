Use the `spec-creator` agent to write a specification for a new "order summary card"
that appears on the customer's order page.

The design for it is in the Figma file we reviewed yesterday — it shows the total, the
item count and the shipping status.
